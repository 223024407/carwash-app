#!/bin/sh
# ----------------------------------------------------------------------------
# Maven Wrapper startup script
# ----------------------------------------------------------------------------
MVNW_HOME="$(cd "$(dirname "$0")" && pwd)"
exec "$MVNW_HOME/.mvn/wrapper/maven-wrapper.jar" "$@"
